# Copy to .env in client/ and set this to your deployed backend's URL.
# Leave it unset/blank to keep using the original offline (localStorage-only,
# single-device, demo-login) mode — the cloud backend is opt-in.
REACT_APP_API_URL=https://your-deployed-backend-url.onrender.com
